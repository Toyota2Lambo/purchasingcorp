.DS_Store
node_modules/
.vercel
.env
.env.*
*.log

# social pipeline — derived/local artifacts (regenerated in CI)
social/pricing.json
social/.photo-cache.json
social/_sample/
social/**/.render-tmp/

# pricing engine — derived/local artifacts (regenerated each run)
pricing-engine/data/
pricing-engine/out/
# ...but keep the env TEMPLATE (the .env.* rule above would hide it)
!pricing-engine/.env.example

# ux-audit — local only, never pushed
ux-audit/
