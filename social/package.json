{
  "name": "purchasingcorp-social",
  "version": "1.0.0",
  "private": true,
  "description": "PurchasingCorp Instagram content renderer (Puppeteer) + template registry.",
  "scripts": {
    "dump-pricing": "node dump_pricing.js",
    "render": "node renderer.js",
    "render:sample": "node renderer.js --sample"
  },
  "dependencies": {
    "puppeteer": "^22.15.0"
  }
}
